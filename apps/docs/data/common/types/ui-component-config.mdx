---
metaTitle: UiComponentConfig Interface
metaDescription: Extends MindDrop's UI using simple configuration objects.
---

# UiComponentConfig

<Description>
  Extend MindDrop's UI using simple configuration objects.
</Description>

MindDrop's user interface can be extended using component configuration objects when your UI needs do not require custom elements.

The various UI configuration interfaces are documented here. These interfaces are referenced throughout the extension docs.

## Interfaces

### NavItemConfig

Used to render a [`PrimaryNavItem`](/docs/ui/components/primary-nav-item) or [`SecondaryNavItem`](/docs/ui/components/secondary-nav-item) in the application sidebar.

<PropsTable
  data={[
    {
      name: 'label',
      type: 'string',
      required: true,
      description: 'The item label.',
    },
    {
      name: 'icon',
      type: 'IconName',
      required: true,
      description: 'The item icon.',
    },
    {
      name: 'onClick',
      typeSimple: 'function',
      type: '(core: Core) => void',
      required: true,
      description:
        'Callback fired when the item is clicked (or activated using the keyboard).',
    },
    {
      name: 'isActive',
      typeSimple: 'function',
      type: '(core: Core) => boolean',
      required: true,
      description:
        'Function used to determine if the item is active.',
    },
  ]}
/>

### IconButtonConfig

Used to render an [`IconButton`](/docs/ui/components/icon-button).

<PropsTable
  data={[
    {
      name: 'icon',
      type: 'IconName',
      required: true,
      description: 'The button icon.',
    },
    {
      name: 'label',
      type: 'string',
      required: true,
      description:
        "Hidden button label for accessibility purposes. Should be the action name (e.g. 'Save').",
    },
    {
      name: 'onClick',
      typeSimple: 'function',
      type: '(core: Core) => void',
      required: true,
      description:
        'Callback fired when the item is clicked (or activated using the keyboard).',
    },
  ]}
/>
