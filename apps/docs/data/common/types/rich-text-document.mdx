---
metaTitle: RTDocument Interface
metaDescription: A document which combines rich text block elements to form a rich text document.
---

# RTDocument

<Description>
  A document which combines rich text block elements to form a rich
  text document.
</Description>

### RTDocument

<InterfaceTable
  data={[
    {
      name: 'id',
      type: 'string',
      required: true,
      description: 'The document ID.',
    },
    {
      name: 'revision',
      type: 'string',
      required: true,
      description:
        'A UUID used to track different versions of the document.',
    },
    {
      name: 'parents',
      type: 'ParentReference[]',
      required: true,
      description: "References to the document's parents.",
    },
    {
      name: 'createdAt',
      type: 'Date',
      required: true,
      description: 'Timestamp at which the document was created.',
    },
    {
      name: 'updatedAt',
      type: 'Date',
      required: true,
      description: (
        <span>
          Timestamp at which the document was last updated. Equal to
          the <Code>createdAt</Code> value if the document has not
          been updated.
        </span>
      ),
    },
    {
      name: 'children',
      type: 'string[]',
      required: true,
      description: (
        <span>
          The IDs of the <Code>RTBlockElement</Code>s which make
          up the content of the document.
        </span>
      ),
    },
    {
      name: 'deleted',
      type: 'true',
      description: (
        <span>
          When <Code>true</Code>, the rich text document is deleted.
          Not present if the document is not deleted.
        </span>
      ),
    },
    {
      name: 'deletedAt',
      type: 'Date',
      description:
        'Timestamp at which the rich text document was deleted. Not present if the document is not deleted.',
    },
  ]}
/>
