---
metaTitle: RichTextMarks API Reference
metaDescription: An interface for retrieving and managing rich text mark configs.
---

# RichTextMarks

<Description>
  An interface for retrieving and managing rich text mark configs.
</Description>

In rich text documents, text nodes are the lowest-level nodes in the tree, containing the text content of the document, along with any formatting.

To implement formatting such as **bold**, text nodes can contain custom properties refered to as 'marks'.

Marks are defined using a [RTMarkConfig](/docs/extensions/types/rich-text-mark-config) object.

## Static methods

### register

Reigsters one or more rich text marks. Dispatches a `rich-text-marks:register` event for each config.


```ts
RichTextMarks.register(
  core: Core,
  config: RTMarkConfig | RTMarkConfig[],
): void
```

<ArgsTable
  core
  data={[
    {
      name: 'config',
      type: 'RTMarkConfig | RTMarkConfig[]',
      required: true,
      description: 'The rich text mark config(s) to reigster.',
    },
  ]}
/>

### unregister

Unregisters a rich text mark by `key` and dispaches a `rich-text-mark:unregister` event.

```ts
RichTextMarks.unregister(core: Core, key: string): void
```

<ArgsTable
  core
  data={[
    {
      name: 'key',
      type: 'string',
      required: true,
      description: 'The key of the rich text mark to unregister.',
    },
  ]}
/>

### get

Retrieves a rich text mark config by key. Returns `undefined` if the mark is not registered.

```ts
RichTextMarks.get(key: string): RTMarkConfig | undefined
```

<ArgsTable
  data={[
    {
      name: 'key',
      type: 'string',
      required: true,
      description: 'The key of the rich text mark for which to retrieve the config.',
    },
  ]}
/>

### getAll

Returns all registered rich text marks.

```ts
RichTextMarks.getAll(): RTMarkConfig[]
```
## RichTextMark events

<EventsTable
  data={[
    {
      name: 'rich-text-mark:register',
      data: 'RTMarkConfig',
      description:
        'Dispatched when a new rich text mark is registered.',
    },
    {
      name: 'rich-text-mark:unregister',
      data: 'RTMarkConfig',
      description:
        'Dispatched when a rich text mark is unregistered.',
    },
  ]}
/>
