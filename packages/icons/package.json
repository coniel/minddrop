{
  "name": "@minddrop/icons",
  "version": "0.1.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsc -p tsconfig.build.json --watch",
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "rimraf": "~3.0.2",
    "ts-node": "^10.4.0",
    "typescript": "~4.4.4"
  },
  "peerDependencies": {
    "react": "17.0.2"
  },
  "dependencies": {
    "@minddrop/utils": "0.1.0"
  }
}
