A fenced block with a language:

```ts
const a = 1;
const b = 2;
```

A fenced block with no language:

```
plain text
```

A tilde fenced block:

~~~python
print("hello")
~~~

A longer fence holding a shorter one:

````
```
nested fence
```
````

A fence with a meta string:

```ts title="example.ts"
export const value = 1;
```

An indented code block:

    indented line one
    indented line two
