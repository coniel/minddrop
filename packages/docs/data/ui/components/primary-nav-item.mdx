---
metaTitle: PrimaryNavItem
metaDescription: A primary navigation element for sidebars.
name: primary-nav-item
---

# PrimaryNavItem

<Description>A primary navigation element for sidebars.</Description>

<HeroContainer>
  <PrimaryNavItemDemo />
</HeroContainer>

```jsx hero template=PrimaryNavItemDemo

```

<Highlights
  features={[
    'Full keyboard navigation.',
    'Applies aria-current="location" when active for accsesibility.',
  ]}
/>

## Usage

Import the component from the `@minddrop/ui` package.

```jsx
import { PrimaryNavItem, Icon } from '@minddrop/ui';

export default () => {
  return <PrimaryNavItem label="Search" icon="search" />;
};
```

## API Reference

`PrimaryNavItemProps` extends `React.HTMLAttributes<HTMLDivElement>`.

<PropsTable
  data={[
    {
      name: 'label',
      type: 'node',
      required: true,
      description: 'The item label.',
    },
    {
      name: 'icon',
      type: 'IconName | ReactElement',
      required: true,
      description: 'The item icon.',
    },
    {
      name: 'active',
      type: 'boolean',
      description: (
        <span>
          If <Code>true</Code>, the item will have active styling and
          accessibility indicators.
        </span>
      ),
    },
    {
      name: 'onClick',
      type: '(event: React.MouseEvent) => void',
      typeSimple: 'function',
      description: 'Callback fired on click or keyboard activation.',
    },
  ]}
/>

## Accessibility

Adheres to the [Button WAI-ARIA design pattern](https://www.w3.org/TR/wai-aria-practices-1.2/#button).

### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Space'],
      description: 'Activates the item.',
    },
    {
      keys: ['Enter'],
      description: 'Activates the item.',
    },
  ]}
/>
