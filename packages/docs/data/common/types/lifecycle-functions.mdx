---
metaTitle: Lifecycle Functions
metaDescription: Extensions are defined using a set of lifecycle functions.
---

# Lifecycle functions

<Description>
  Extensions are defined using a set of lifecycle functions.
</Description>

MindDrop extensions are defined using a set of lifecycle functions. The app calls these functions at given stages of the extension's life or in response to certain events.

<FunctionsTable
  data={[
    {
      name: 'onRun',
      typeSimple: 'function',
      type: '(app: App) => void',
      required: true,
      description:
        'The main extension function and the only required one. Called when the app initializes the extension on startup. Use this function to set up event listeners.',
    },
    {
      name: 'onInstall',
      typeSimple: 'function',
      type: '(app: App) => Promise<void>',
      description: (
        <span>
          Called once when the extension is installed. Directly
          followed by a call to <Code>onActivate</Code>.
        </span>
      ),
    },
    {
      name: 'onActivate',
      typeSimple: 'function',
      type: '(app: App) => Promise<void>',
      description: (
        <span>
          Called once when the extension is activated globally. Called
          directly after <Code>onInstall</Code>, but can also be
          called independently at a later stage if the extension was
          temporarily disabled.
        </span>
      ),
    },
    {
      name: 'onUpdate',
      typeSimple: 'function',
      type: '(app: App) => void',
      description: (
        <span>
          Called once when the extension has been updated to a new
          version. Followed by a call to <Code>onActivate</Code>.
        </span>
      ),
    },
    {
      name: 'onDeactivate',
      typeSimple: 'function',
      type: '(app: App) => Promise<void>',
      description: (
        <span>
          Called once when the extension is deactivated globally.
          Called directly before <Code>onUninstall</Code>, but can
          also be called independently if the user disables the
          extension without uninstalling it.
        </span>
      ),
    },
    {
      name: 'onUninstall',
      typeSimple: 'function',
      type: '(app: App) => Promise<void>',
      description: (
        <span>
          Called once when the extension is uninstalled. Use to clean
          up any data stored by your extension. Always preceeded by a
          call to <Code>onDeactivate</Code>.
        </span>
      ),
    },
    {
      name: 'onTopicActivate',
      typeSimple: 'function',
      type: '(app: App, topic: Topic) => Promise<void>',
      description:
        'Called once when the extension is activaed for a given topic.',
    },
    {
      name: 'onTopicDeactivate',
      typeSimple: 'function',
      type: '(app: App, topic: Topic) => Promise<void>',
      description:
        'Called once when the extension is deactivaed for a given topic.',
    },
  ]}
/>
