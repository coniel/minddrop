---
metaTitle: Extension App API Interface
metaDescription: The primary interface through which extensions interact with the application.
---

# ExtensionAppApi

<Description>
  The primary interface through which extensions interact with the
  application.
</Description>

### Methods

<FunctionsTable
  data={[
    {
      name: 'addEventListener',
      type: '(type: EventType, callback: EventCallback) => void',
      description:
        'Allows extensions to listen to events occuring throughout the app. See the EventListener types page for avaliable events.',
    },
    {
      name: 'dispatch',
      type: '(type: EventType, data: EventData) => void',
      description:
        'Allows extensions to dispatch app wide events. See the EventListener types page for avaliable events.',
    },
    {
      name: 'extendUi',
      type: `(location: UILocation, element: UIComponentConfig | ReactElement) => void`,
    },
    {
      name: 'extendEditor',
      type: '(editorId: string, extension: EditorExtension) => void',
    },
    {
      name: 'getTopic',
      type: '(id: string) => Topic | null',
      description:
        'Fetches a topic by ID. Returns the topic or null if not found.',
    },
    {
      name: 'getDrop',
      type: '(id: string) => Drop | null',
      description:
        'Fetches a drop by ID. Returns the drop or null if not found.',
    },
  ]}
/>
