/**
 * Key used to store selection items in clipboard and drag event data.
 */
export const SELECTION_DATA_KEY = 'application/minddrop-selection';

/**
 * Key used to store the action type in clipboard and drag event data.
 */
export const ACTION_DATA_KEY = 'application/minddrop-action';
