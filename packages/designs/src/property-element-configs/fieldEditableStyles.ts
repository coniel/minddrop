/**
 * The style keys a field-rendering editor variant offers for
 * editing. Only the margins are per-element: the value's
 * typography and the field box chrome (colour, background,
 * border, padding) are the field look the theme controls. The
 * label and icon keys enable the property chrome sections.
 */
export const FieldEditableStyles = [
  'label',
  'icon',
  'marginTop',
  'marginRight',
  'marginBottom',
  'marginLeft',
];
