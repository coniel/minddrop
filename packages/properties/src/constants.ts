import { PropertyType } from './types';

export const FileBasedPropertyTypes: PropertyType[] = ['image', 'file'];

export const FilePropertySupportedFileExtensions = {
  image: ['jpg', 'jpeg', 'png', 'gif', 'bmp', 'svg', 'webp', 'ico'],
  file: [],
};
