export const WorkspacesConfigFileName = 'workspaces.json';

/**
 * The name of the directory inside the app data directory under
 * which each workspace's data directory is created, keyed by
 * workspace ID.
 */
export const WorkspacesDataDirName = 'workspaces';

export const DefaultWorkspaceIcon = 'lucide:box:default';
