import { BaseItemTypeConfig } from '../types';

export const PageBaseItemTypeConfig: BaseItemTypeConfig = {
  type: 'page',
  dataType: 'page',
  name: 'Page',
  description: 'A base item type for page based content.',
  icon: 'content-icon:panel-top',
  properties: [],
};
