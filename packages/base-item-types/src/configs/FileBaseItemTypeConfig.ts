import { BaseItemTypeConfig } from '../types';

export const FileBaseItemTypeConfig: BaseItemTypeConfig = {
  type: 'file',
  dataType: 'file',
  name: 'File',
  description: 'A base item type for file based content.',
  icon: 'content-icon:file-box',
  properties: [],
};
