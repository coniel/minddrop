import { BaseItemTypeConfig } from '../types';

export const AudioItemTypeConfig: BaseItemTypeConfig = {
  type: 'audio',
  dataType: 'audio',
  name: 'Audio',
  description: 'A base item type for audio file based content.',
  icon: 'content-icon:headphones',
  properties: [],
};
