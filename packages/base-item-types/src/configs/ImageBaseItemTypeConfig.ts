import { BaseItemTypeConfig } from '../types';

export const ImageBaseItemTypeConfig: BaseItemTypeConfig = {
  type: 'image',
  dataType: 'image',
  name: 'Image',
  description: 'A base item type for image file based content.',
  icon: 'content-icon:image',
  properties: [],
};
