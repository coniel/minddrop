/**
 * Key prefix for data entries in DataTransfer objects.
 */
export const MindDropDataKey = 'application/vnd.minddrop';

/**
 * Pixel step that requested image display widths are rounded up to.
 */
export const IMAGE_WIDTH_BRACKET = 200;

/**
 * Largest bracketed width for which a resized variant is generated.
 * Above this, the original image is served.
 */
export const MAX_IMAGE_RESIZE_WIDTH = 2400;
