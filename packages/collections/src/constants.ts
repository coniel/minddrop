import type { UiIconName } from '@minddrop/ui-icons';

export const CollectionsDirName = 'collections';
export const CollectionFileExtension = 'json';

/**
 * The icon used to represent collections in the UI.
 */
export const CollectionsIcon: UiIconName = 'library';

/**
 * The default icon assigned to newly created collections.
 */
export const DefaultCollectionIcon = 'content-icon:library:default';
