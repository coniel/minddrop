import { describe, beforeEach, afterEach, it, expect } from 'vitest';
import { removeNodeFromBoard } from './removeNodeFromBoard';

describe('removeNodeFromBoardDebounced', () => {
  it('does something useful', () => {
    expect(1).toBe(1);
  });
});
