---
title: 'New locale keys need the resource types regenerated by hand'
package: packages/i18n
summary: 'After editing a locale JSON run bun packages/scripts/generate-i18n-types.ts and commit i18n-resources.d.ts'
paths:
  - 'packages/i18n/src/locales/**'
  - 'packages/i18n/src/i18n-resources.d.ts'
  - 'packages/scripts/generate-i18n-types.ts'
tags: [i18n, codegen, locales, translation-key]
---

# New locale keys need the resource types regenerated by hand

`TranslationKey` is derived from `src/i18n-resources.d.ts`, which is generated from the locale JSON rather than read from it. Adding a key to `src/locales/en-GB.json` therefore does nothing on its own: the key stays outside the union and every use of it fails to typecheck, with an error that reads as though the key were misspelled ("Did you mean `dataViews.canvas.name`?") rather than ungenerated.

Regenerate after editing a locale file:

```
bun packages/scripts/generate-i18n-types.ts
```

It is a Bun script (`import { Glob } from 'bun'`), so `npx tsx` fails on the import. No package script wraps it, and nothing in the build runs it, so the generated file has to be committed alongside the locale change.
