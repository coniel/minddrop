export interface EntityGroupDragSource {
  /**
   * The type of the group the items were dragged out of.
   */
  type: string;

  /**
   * The ID of the group the items were dragged out of.
   */
  groupId: string;
}
