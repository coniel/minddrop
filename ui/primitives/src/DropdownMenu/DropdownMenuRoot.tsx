import { Menu as MenuPrimitive } from '@base-ui/react/menu';

/* --- DropdownMenuRoot ---
   The raw Base UI root — use for advanced composition.
   For simple cases, use the convenience DropdownMenu instead. */

export type DropdownMenuRootProps = MenuPrimitive.Root.Props;

export const DropdownMenuRoot = MenuPrimitive.Root;
