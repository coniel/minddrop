import { Combobox as ComboboxPrimitive } from '@base-ui/react/combobox';

/* --- ComboboxChips ---
   Re-export of Base UI's Combobox.Chips.
   Container for chip elements with keyboard navigation. */

export type ComboboxChipsProps = ComboboxPrimitive.Chips.Props;

export const ComboboxChips = ComboboxPrimitive.Chips;
