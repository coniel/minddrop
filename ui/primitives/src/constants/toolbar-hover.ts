/**
 * Attributes an open hover menu or panel marks its host toolbar
 * with: the corners it runs through, which the toolbar squares off
 * so the shape the two make has no notch in it, and the controls it
 * covers, which step back while it is open.
 */
export const ToolbarHoverSquaredCornersAttribute = 'data-hover-menu-squared';
export const ToolbarHoverDimmedControlAttribute = 'data-hover-menu-dimmed';
