/**
 * Attribute stamped on the document element with the input modality
 * the user is currently interacting through. Absent until the first
 * interaction.
 */
export const INPUT_MODE_ATTRIBUTE = 'data-input-mode';
