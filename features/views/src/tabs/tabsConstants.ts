import { UiIconName } from '@minddrop/ui-icons';

/**
 * The default tab icon used when a view provides none.
 */
export const DEFAULT_ICON: UiIconName = 'file';

/**
 * The number of leading tabs that can be activated with a numbered
 * shortcut.
 */
export const MAX_SHORTCUT_TABS = 9;

/**
 * How long the modifier key must be held before tabs show their
 * shortcut numbers, in milliseconds.
 */
export const SHORTCUT_NUMBERS_DELAY = 300;
