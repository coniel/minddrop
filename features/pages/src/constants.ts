// Element types offered in the page edit mode palette: static
// capable content and media elements plus layout containers.
// Property bound only types (editor, webview) are excluded, and
// view elements are not yet supported on pages.
export const PAGE_ELEMENT_TYPES = [
  'text',
  'formatted-text',
  'number',
  'date',
  'url',
  'badges',
  'icon',
  'image',
  'image-viewer',
  'container',
];

// Width of the edit mode left panel, matched by the nav toolbar
export const EDIT_PANEL_WIDTH = 300;
