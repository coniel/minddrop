import { TranslationKey } from '@minddrop/i18n';
import { UiIconName } from '@minddrop/ui-icons';

/**
 * The icon representing the settings view, shared by every
 * surface opening it.
 */
export const SettingsIcon: UiIconName = 'settings';

export const SettingsViewName = 'settings:view:settings';
export const SettingsViewId = 'settings:settings';

// The settings view's tab title
export const SettingsViewTitle = 'settings.title' satisfies TranslationKey;
