import { afterEach, describe, expect, it } from 'vitest';
import { cleanup, render } from '@minddrop/test-utils';
import { ItemTypeView } from './ItemTypeView';

describe('<ItemTypeView />', () => {
  afterEach(cleanup);

  it('renders the className', () => {});
});
