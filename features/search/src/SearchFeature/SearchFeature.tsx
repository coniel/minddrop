import React from 'react';
import { SearchDialog } from '../SearchDialog';

/**
 * Renders the top-level search feature UI.
 */
export const SearchFeature: React.FC = () => {
  return <SearchDialog />;
};
